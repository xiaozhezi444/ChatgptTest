# ChatgptTest

This repository contains a simple Snake game implemented in Python using the `curses` library.

## How to run

Ensure you have Python 3 installed. From this directory, run:

```bash
python3 snake.py
```

Use the arrow keys to control the snake. Eat the `*` food pieces to grow and earn points. Press `q` to quit the game.

